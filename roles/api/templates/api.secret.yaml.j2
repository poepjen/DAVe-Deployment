# The secrets should be base64 encoded
apiVersion: v1
kind: Secret
metadata:
  name: dave-api
  namespace: {{ namespace }}
  labels:
    app: dave
    layer: api
    project: dave
data:
  http.auth_salt: XXX
  http.private_key: XXX
  http.public_key: XXX
  jwt.keystore: XXX
  jwt.keystore_password: XXX
