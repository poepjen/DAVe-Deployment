kind: NetworkPolicy
apiVersion: extensions/v1beta1
metadata:
  name: api-access
  namespace: {{ namespace }}
spec:
  podSelector:
    matchLabels:
      app: dave
      layer: api
      project: dave
  ingress:
    - from:
#      - podSelector:
#          matchLabels:
#            run: busybox
